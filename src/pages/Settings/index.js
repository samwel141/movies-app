import React from 'react';
import { Typography } from '@mui/material';

const Settings = () => {
    return (
        <div>
            <Typography variant="h4">Settings Content</Typography>
            {/* Add your settings controls or preferences here */}
        </div>
    );
};

export default Settings;
